import UsersController from './UsersController';

describe('UsersController - create', () => {
  it('Deve conferir se os parâmetros passados no request são válidos', () => {});

  it('Não deve permitir inserir emails repetidos', () => {});
});
